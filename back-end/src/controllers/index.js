const authController = require('./authController');
const taskController = require('./taskController');
const submissionController = require('./submissionController');
const reputationController = require('./reputationController');
const escrowController = require('./escrowController');
const computeController = require('./computeController');

module.exports = {
    authController,
    taskController,
    submissionController,
    reputationController,
    escrowController,
    computeController
};